const int read_analog_pin = ;#need to specify the pin of read analog.

void setup(){
	pinMode(read_analgo_pin, INPUT);
	Serial.begin(9600)
}

void loop(){
	OUTPUT = analogRead(gread_analgo_pin)
	Serial.begin(OUTPUT)
	dealy(1000)
	
}